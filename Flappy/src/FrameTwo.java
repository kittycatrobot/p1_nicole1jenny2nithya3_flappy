
public class FrameTwo {

    private boolean paused;
	
    if(gameorwhateverwecallit == pause) {
    	Rectangle r = new Rect(then whatever elements we need to put here 4 a pause screen);
    }
	
    
    //also need to make a reset for the bird when it hits smtg

    public void reset() {
        x = screenwidth/2;
        y = screenlength/2;
        vx = vy = 0;
        
    }
	  public boolean paused() {
	        return paused;
	    }
}
//make sure when the pause button is clicked again u can resume playing, i just dont know how to add it